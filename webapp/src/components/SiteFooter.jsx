import Brand from './Brand';

export default function SiteFooter({ onNavigate, onHomeSection }) {
  return <footer className="site-footer"><div className="container footer-grid"><div className="footer-intro"><button onClick={() => onNavigate('/')}><Brand /></button><p>See through the lies. A clearer way to review digital content.</p></div><div><h3>Verify</h3><button onClick={() => onNavigate('/verify/text')}>Text</button><button onClick={() => onNavigate('/verify/image')}>Image</button><button onClick={() => onNavigate('/verify/video')}>Video</button><button onClick={() => onNavigate('/verify/audio')}>Audio</button></div><div><h3>Explore</h3><button onClick={() => onHomeSection('how-it-works')}>How it works</button><button onClick={() => onNavigate('/about')}>About</button><button onClick={() => onNavigate('/pricing')}>Pricing</button><button onClick={() => onNavigate('/faq')}>FAQ</button></div><div><h3>A clear note</h3><p>TruthLens surfaces automated assessment signals. It does not guarantee the authenticity of submitted content.</p></div></div><div className="container footer-bottom"><span>© 2026 TruthLens AI</span><span>Built for clearer decisions.</span></div></footer>;
}
